-- appearance.
  hl.config({
    general = {
      gaps_in  = 4, -- 10 for wide
      gaps_out = 10, -- 20 for wide
      resize_on_border = false,
      allow_tearing = false,
      layout = master,
      border_size = 0,
    },

    animations = {
      enabled = false,
    },
  })
